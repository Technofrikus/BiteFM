name: BiteFM
options:
  bundleIdPrefix: com.technofrikus
settings:
  configs:
    Debug:
      ENABLE_DEBUG_DYLIB: NO
schemes:
  BiteFM:
    build:
      targets:
        BiteFM: [run, test, profile, analyze, archive]
    run:
      config: Debug
    test:
      config: Debug
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release
targets:
  BiteFM:
    type: application
    platform: macOS
    deploymentTarget: "14.0"
    settings:
      configs:
        Debug:
          ENABLE_DEBUG_DYLIB: NO
      PRODUCT_NAME: "BiteFM"
    sources: [Sources/BiteFM]
    info:
      path: Info.plist
      properties:
        LSMinimumSystemVersion: "14.0"
        NSHighResolutionCapable: true
        NSPrincipalClass: NSApplication
        CFBundleDisplayName: "BiteFM"

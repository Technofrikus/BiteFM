name: HoersaalB
options:
  bundleIdPrefix: com.technofrikus
settings:
  configs:
    Debug:
      ENABLE_DEBUG_DYLIB: NO
schemes:
  HoersaalB:
    build:
      targets:
        HoersaalB: [run, test, profile, analyze, archive]
    run:
      config: Debug
    test:
      config: Debug
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release
targets:
  HoersaalB:
    type: application
    platform: macOS
    deploymentTarget: "14.0"
    settings:
      configs:
        Debug:
          ENABLE_DEBUG_DYLIB: NO
      PRODUCT_NAME: "Hörsaal B"
    sources: [Sources/HoersaalB]
    info:
      path: Info.plist
      properties:
        LSMinimumSystemVersion: "14.0"
        NSHighResolutionCapable: true
        NSPrincipalClass: NSApplication
        CFBundleDisplayName: "Hörsaal B"
